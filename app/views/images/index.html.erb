<h4>All My Images</h4>
<% @images.each do |f| %>
<%= link_to (image_tag f.image_url(:thumb)), user_image_path(@user ,f) %>
<% end %>
</br>
<% if is_admin? %>
<%= link_to "Add Image", new_user_image_path(@user) %>
<% end %>
